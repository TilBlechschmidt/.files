export alias k = kubectl

# These are replaced by the K8s module
#
# export alias kg = kubectl get
# export alias kd = kubectl describe
# export alias kl = kubectl logs
# export alias kc = kubectl config
# export alias kdel = kubectl delete
# export alias kcns = kubectl config set-context --current --namespace
# export def kt [] { 
#    align | lines | str trim | split column ' ' --collapse-empty | headers 
# }
